    
node{
  stage('SCM Checkout') {
    git 'https://github.com/moharir/Nirwana'
    }
    stage('Compile-Package'){
    def mvnHome = tool name: 'Maven 3.6.1', type: 'maven'
      sh "${mvnHome}/bin/mvn package"
    }
}
